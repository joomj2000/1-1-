#include <iostream>
#include <algorithm>
#include <string>
#include<vector>
#include <cmath>
#include <list>
using namespace std;


int main() {

    ios::sync_with_stdio(0);
    cin.tie(0);
    int N;
    string str;
    

    cin >> N;
    while (N--) {
        cin >> str;
        if (str.compare("anj") == 0) { cout << "뭐야;";return 0; }
   }
    cout << "뭐야?";
} 